# SGB, printer, and video candidate evidence (2026-09-26)

## Status and scope

This is an independent evidence ledger for an unreleased integration candidate, not release notes or hardware certification. Integration worktree is `fix/sgb-printer-video-prerelease`; BASE is `763f6bfab8d7f3bed164976277516954141c1618`. Current tested source SHA is `a0846aae39fc9999ddda34269de317429dee85d1`. Exact-SHA core `36182509783`, ordinary DS `36182509785`, full preflight `36182535946`, and separate `VIDEO_TRACE=1` DS `36182538849` all PASS. Preflight includes normal, UBSan, ASan+LSan, 27 host tests, resources/package checks and two clean pinned BlocksDS 1.22.2 DS/DSi builds; downloaded normal and diagnostic artifacts pass `check_repository.py --build-dir` structural checks. The targeted final review found the prior P1/P2 issues corrected and no new code blocker; this is not a release approval. Candidate `53692f5a09b3c7006040d7f2ac2ed1df3bb8f216` is historical: although its listed CI passed, review found a probe lifecycle save regression and a diagnostic trace menu-history defect. Base preflight `35911092936` (core `35911086494`, DS `35911086437`) is BASE-only, not candidate evidence. Galmuri regeneration was not run.

User reports: Nintendo 3DS in DS mode with DSpico; Tales of Phantasia: Narikiri Dungeon Korean SHA-256 `c004b5ade81fd35e441dc74b133bf2dc57bd2bad7079c8f041ea217d6c9f063e` (header CGB `0x80`, SGB `0x03`), **GB Printer was On**, but the printer submenu's Print action made no progress; Scaling-on can cause a brief full-screen black display. SGB Borders On slowdown and fast-forward interaction are user-reported; current SGB Borders Off was clarified as a workaround after the slowdown. The earlier Custom Border value is unknown. Castlevania Legends Korean SHA-256 `3ed5f6aae59a7ff770c687eee593edb2d5faae172775d76d7786a4820d9a6a2a` (CGB `0x20`, SGB `0x03`) is the SGB-capable non-CGB case. Current config additionally maps Printer/Scaling/Scale Filter On, Wait for Vblank Off, Hblank/Window On; it does not explain the reported flicker. ROMs remain user-owned/read-only; this report contains hashes, not game data. Earlier ROM reload freeze is USER_REPORTED resolved; exact personal binary SHA is unknown, so no retrospective hardware certification is assigned. Requested model/effort was Luna/medium, but actual resolved model/effort metadata is UNVERIFIED.

## Independent acceptance matrix

| Area / cell | Expected | Evidence available at this draft | Classification / next gate |
|---|---|---|---|
| SGB idle-host startup, no host program | Host CPU/APU remain idle rather than consuming repeated frame budgets; SGB transfer that provides a program resumes execution | Video owner's Windows `clang++` 22.1.8 run of `tests/sgb_host_idle_test.cpp` + production `platform/common/sgb_host.cpp`: BASE exit 1 after 4 frames (`apu_pc=16384`, APU/CPU unstopped); candidate commit `6ded8138c75b282ba90b9b743e2300aad1f6cddf` exit 0. Synthetic SOU_TRN wakes APU; JUMP/STP covered. Existing host/baseline/65C816 vectors exit 0; vector executable needed Windows linker stack reserve 8 MiB. Raw log/command not included in supplied excerpt | Host synthetic PASS as owner-reported; does not measure DS game speed, audio, input, or prove user's original slowdown fixed. Candidate integrated rerun required |
| Border-probe persistent-state isolation | Temporary extraction must not mutate SRAM/RTC or leave autosave dirtiness; mode changes/reset/cancel/timeout restore the selected game's state | Windows Clang production-linked MBC3 RAM+RTC test. BASE pre-fix exit 4: SRAM expected `5a`, actual `a5`. At historical `53692f5`, file-backed test exited 44 when reset selected real SGB and save was attempted. At `a0846aae`, it exits 0 across `loadSave(1)`, SGB/GBC mode changes + reset, SRAM/RTC save and reload, repeated reset, 450-VBlank timeout, border-setting cancellation, and 0/8/128 KiB RAM; allocation-failure and interrupted-unload cases also pass. No user save or ROM opened | Regression reproduced and corrected in production-linked host harness; no DS/game run. Snapshot allocation failure skips probe safely |
| Fast-forward with SGB border probe | Hold and toggle fast-forward inputs must be serviced while probe is active; after probe, GBC/GB timing and limiter remain correct | Windows Clang `sgb_probe_host_input_test.exe` exit 0: hold/toggle/menu host control during probe; guest buttons remain neutral. `sgb_host_idle_test.exe` and exact-source CI pass. No guest-cycles/real-time or displayed-frame measurement; no DS test | Host input path PASS; user-reported SGB Borders On slowdown/fast-forward interaction not performance-measured. No speed percentage/guarantee. Representative game/mode hardware matrix pending |
| Flicker / scaling black screen | Only complete frames publish; no premature clear/disable or unsafe cross-core VRAM/DMA ownership transfer | User report: brief full-screen black while scaling enabled. VBlank queue tests cover capacity/overflow/order/reentrant scheduling; host fixed-ring tests cover overflow/mixed-generation detection. `VIDEO_TRACE=1` diagnostic NDS build at A084 passes exact-SHA CI/build and structural checks. Later review found trace sampling continued while opening menus; A084 freezes it at menu open and resumes at close. Only about 0.2 seconds of pre-menu history remains; slow capture/late menu makes absence inconclusive. No hardware launch or CSV capture. ARM7/ARM9 remap remains an unverified source hypothesis | USER_REPORTED symptom; cause UNCONFIRMED, no cause-linked fix or integrated flicker reproduction. Queue/ring tests and build do not prove DS flicker fixed or DMA safety |
| Printer parser + BMP synthetic path | Valid packet sequence creates expected BMP pixels; invalid checksum/length/RLE cannot mutate retained image or trigger print; failed writes preserve previous output | RLE regression fixed before A084: two-pass code validates checksum/RLE/expanded length against remaining 8,000-byte capacity, then decodes directly to image tail. Synthetic coverage includes 12 encoded bytes→774 decoded, exact 8,000-byte image, over-cap rejection, valid `0xFF`→129 per Nintendo manual p.244, and valid-prefix/truncated-tail transactional preservation; parser/BMP tests cover header/pixels and injected short-write/flush/rename failures, retained prior file and retry. Current production serial test covers INIT/STATUS/DATA/end/PRINT through GB/CGB FF01/FF02 and BMP | RLE and transactional-output scoped tests pass; direct-stdio host test is not production DS FAT/FileHandle/Unicode/CP949/8.3 or actual Tales game/device evidence |
| Printer FF01/FF02 / serial completion | Internal printer transfer completes and clears SC start bit; linked Game Boy peer pending transfers keep their start bit until their own transfer completion | Production-linked GB/CGB FF01/FF02→`runEmul`→printer parser test asserts linked peer SC remains armed before exchange, then peer SC/SB completion; synthetic printer INIT/STATUS/DATA/end/PRINT→BMP and one IRQ/no second-frame IRQ pass. A prior overly broad SC-clear candidate was rejected and narrowed before current source | Scoped host integration test and exact-SHA CI pass. No actual Tales menu, physical DS timing or device test. Host harness uses test platform/FileHandle boundary |
| Printer output path | Storage basename captured at ROM selection; successful staged output preserves old file on failure and survives menu cwd changes | Synthetic Windows temp directory output; expected names `<ROM-storage-basename>-N.bmp`, staged through `.tmp` before replacement. Test injects short write/flush/rename failures and checks retention. Diagnostics require **Console Output → Debug**; `printLog` has no guaranteed visible error UI. No production DS `FileHandle`/FAT, Unicode/CP949/8.3 alias or actual SD-card write | Host filesystem behavior only; real DS FileHandle/FAT/SD and user's game NOT RUN. Never use user printouts for write tests |
| Legacy lifecycle / sanitizer / package / DS builds | BASE regressions stay passing on the final candidate, with required checks repeated at exact source SHA | At A084: core `36182509783`, ordinary DS `36182509785`, preflight `36182535946`, separate diagnostic DS `36182538849` all PASS. Preflight includes normal/UBSan/ASan+LSan, 27 host tests, resources/package, two clean pinned BlocksDS 1.22.2 DS/DSi builds and structural checks. Full Galmuri regeneration NOT RUN. | Named software/build gates pass at exact source SHA; no real-device/full-game evidence. Hardware remains pending |

## Build identity and remaining release gates

At source `a0846aae39fc9999ddda34269de317429dee85d1`, normal NDS artifacts are 731,648 bytes: `gameyob.nds` SHA-256 `fea746ef1105b60f408f2b44dd70216b69d3a995329450b69436eae77366a756`; `gameyob_dsi.nds` `2d0e54c33bd1fccf74261beaf39fb2a0461c5ac7e49fb8bbc39cb47f7ededbe1`. Diagnostic artifacts are 733,696 bytes: `gameyob.nds` SHA-256 `d04268f2f5f475ff163c515d422b8f985ebf898c822883200da1bd9474489788`; `gameyob_dsi.nds` `3a8d36892be8f00e7f59044de290a724eccad9580435f65bd0a49b5febea6423`. The private diagnostic ZIP with notices is 799,403 bytes, SHA-256 `ab2e1e2e1fb7fb0525ac51638f88fc8c714db2acfd0b773fdc85caebd1096bfb`; extraction verified all eight manifest entries. This metadata does not authorize redistribution or constitute legal approval. Earlier candidates `d73d706` and `53692f5` and their packages are historical, not current artifacts; `53692f5` passed then-available CI but was rejected after targeted review found the save-lifecycle and trace-history defects corrected at A084. Diagnostic artifacts are distinct from normal emulator output and are not hardware validation.

User-facing performance measurement (guest cycles/frames per real second, display FPS, waits, CPU load) was not run, so no speed percentage is supported. No actual DS/3DS game launch or Tales menu print attempt, production FAT/Unicode/CP949/8.3 path test, or diagnostic CSV hardware capture was recorded. Diagnostic capture is build-verified only; hardware test remains pending. Outcome: `PARTIAL_FIX_DIAGNOSTICS_READY`; no tag or release. Scaling flicker remains USER_REPORTED with cause UNCONFIRMED and no cause-linked safe correction, a publication blocker. The repository's inherited provenance note at `docs/maintenance/repository-preflight-20260903.md` lines 223–226 records `3in1.*` attribution without a separate grant. No new dependencies/assets were added, and existing notices are included in the private package; this report makes no legal-clearance or redistribution determination. That unresolved provenance is also a publication gate pending maintainer resolution.

References: Pan Docs Game Boy Printer page at repository revision `e196504` (2026-04-19), <https://gbdev.io/pandocs/Gameboy_Printer.html>; Nintendo *Game Boy Programming Manual*, Chapter 9 printed page 244, <https://thissideout.wordpress.com/wp-content/uploads/2014/02/gameboyprogrammingmanual.pdf>. The Pan Docs page was search-index accessible but direct fetch returned HTTP 403 during this audit; checked-in revision/source was used. It specifies packet fields, checksum over command/flags/length/payload (excluding magic/checksum), status after the two trailing zero bytes, INIT buffer clear, DATA chunks up to `0x280` bytes, PRINT's four parameters, zero-sheet line feed, required empty DATA packet before PRINT, 8 KiB printer RAM / 160×200 maximum bitmap area, and status-bit meanings. The Nintendo manual Chapter 9 p.244 confirms `0xFF` is a valid RLE control repeating the next byte 129 times; the earlier owned-worktree parser rejection was incorrect and was corrected in integration source and fixture. Pan Docs itself leaves compression details incomplete.

The parser intentionally has bounded packet/image storage, checksum validation before command effects, and staged BMP replacement according to its synthetic tests. Current implementation rejects sheet counts other than one, creates a separate BMP for each accepted PRINT, and does not render margins/exposure as paper effects. The d73 decoder's 640-byte expanded-packet cap was corrected before A084 with two-pass validate-then-decode, bounded by remaining 8,000-byte image capacity; current source and CI include above-640, total-bound and truncation-preservation tests. It models only the status subset implemented in code; it does not claim physical battery, jam, thermal, paper, or timing behavior. Do not describe broader hardware compatibility.

## Required evidence to close

Windows Clang tests are owner-reported; individual raw logs and repeat counts are not attached. A084 includes the linked-peer pending-transfer counterfactual, RLE 640-byte-cap correction, mode-change/save lifecycle regression and trace menu-history correction. Remaining distinctions: user-reported Tales game/menu flow; host filesystem versus DS FAT/FileHandle; SGB guest progress versus display FPS and limiter waits; intended/filter/camera blacking versus incomplete frame publication; DS host ordering versus physical VRAM/DMA timing. Diagnostic build and menu dump path passed exact-SHA build/binary checks, but hardware launch or CSV capture did not occur. Ring holds 256 events and menu freeze retains only about 0.2 seconds before opening; absence after slow capture is inconclusive. Hardware remains pending. The absence of a game run or trace is NOT RUN, never PASS.

## Printer protocol and implementation boundary

Disposition is `PARTIAL_FIX_DIAGNOSTICS_READY`, not release-ready. Exact-source software checks and targeted final review pass, but scaling flicker still has no established cause or cause-linked fix, hardware/game paths remain unverified, and inherited `3in1.*` provenance lacks a separately established grant. These are publication gates; notices/package inclusion is not legal approval. No version/tag/publish decision is made here.
