# SGB, printer, and video candidate evidence (2026-09-26)

## Status and scope

This is an independent evidence ledger for the unreleased integration candidate, not release notes or a hardware certification. Integration worktree is `fix/sgb-printer-video-prerelease`. BASE is `763f6bfab8d7f3bed164976277516954141c1618`. Current source SHA is `53692f5a09b3c7006040d7f2ac2ed1df3bb8f216`; it includes a two-pass decoded RLE length fix and follows d73's correction to initially overbroad SC completion handling. At exact current SHA, core jobs `36180780686`, `36180775281`, DS jobs `36180780454`, `36180775148`, preflight `36180797959`, and separate `VIDEO_TRACE=1` DS build `36180800999` all PASS. Preflight includes normal/UBSan/ASan+LSan, 27 host tests, resources/package, two pinned BlocksDS builds and binary/header/embedded-asset checks. Current artifact hashes are recorded below; d73 hashes below are historical only. The final documentation edits remain outside the code commit pending coordinator decision. Base preflight `35911092936` (core `35911086494`, DS build `35911086437`) is PASS at BASE only and not candidate evidence.

User reports: Nintendo 3DS in DS mode with DSpico; Tales of Phantasia: Narikiri Dungeon Korean SHA-256 `c004b5ade81fd35e441dc74b133bf2dc57bd2bad7079c8f041ea217d6c9f063e` (header CGB `0x80`, SGB `0x03`), **GB Printer was On**, but the printer submenu's Print action made no progress; scaling can cause a brief full-screen black display. Castlevania Legends Korean SHA-256 `3ed5f6aae59a7ff770c687eee593edb2d5faae172775d76d7786a4820d9a6a2a` (CGB `0x20`, SGB `0x03`) is the SGB-capable non-CGB case. ROMs remain user-owned/read-only; this report contains hashes, not game data. No incident video or exact tested binary hash was supplied. Earlier ROM reload freeze is USER_REPORTED resolved; exact personal binary SHA is unknown, so no retrospective hardware certification is assigned.

## Independent acceptance matrix

| Area / cell | Expected | Evidence available at this draft | Classification / next gate |
|---|---|---|---|
| SGB idle-host startup, no host program | Host CPU/APU remain idle rather than consuming repeated frame budgets; SGB transfer that provides a program resumes execution | Video owner's Windows `clang++` 22.1.8 run of `tests/sgb_host_idle_test.cpp` + production `platform/common/sgb_host.cpp`: BASE exit 1 after 4 frames (`apu_pc=16384`, APU/CPU unstopped); candidate commit `6ded8138c75b282ba90b9b743e2300aad1f6cddf` exit 0. Synthetic SOU_TRN wakes APU; JUMP/STP covered. Existing host/baseline/65C816 vectors exit 0; vector executable needed Windows linker stack reserve 8 MiB. Raw log/command not included in supplied excerpt | Host synthetic PASS as owner-reported; does not measure DS game speed, audio, input, or prove user's original slowdown fixed. Candidate integrated rerun required |
| Border-probe persistent-state isolation | Temporary extraction must not mutate SRAM/RTC or leave autosave dirtiness; interrupted/failed probes restore the active game correctly | Windows Clang production-linked MBC3 RAM+RTC dual-mode test. BASE exit 4: SRAM expected `5a`, actual `a5`. Candidate `sgb_probe_save_isolation_test.exe` exit 0 across RAM sizes 0/8/128 KiB × PCT-like success/450-VBlank timeout/interrupted unload/border-setting cancellation (12 cells), plus forced snapshot-allocation failure (13th cell). Integration snapshots RAM, clock/latches/dirty metadata and restores on final init/interrupted unload; raw per-cell log not attached | BASE FAIL reproduced; scoped candidate synthetic PASS. Host harness boundary; ROM-swap and DS game behavior not tested |
| Fast-forward with SGB border probe | Hold and toggle fast-forward inputs must be serviced while probe is active; after probe, GBC/GB timing and limiter remain correct | Windows Clang `sgb_probe_host_input_test.exe` exit 0: hold/toggle/menu host control during probe; guest buttons remain neutral. `sgb_host_idle_test.exe` exit 0 on integration candidate. No guest-cycles/real-time or displayed-frame measurement; no DS test | Input handling host PASS, actual speed/performance impact unmeasured. Do not report percentage or guarantee. Border off/on × normal/hold/toggle on representative header combinations still pending |
| Flicker / scaling black screen | Only complete frames publish; no premature clear/disable or unsafe cross-core VRAM/DMA ownership transfer | User report: brief full-screen black while scaling enabled. `tests/vblank_task_queue_test.cpp` exit 0 on Windows `clang++` covers capacity/overflow/batch order/reentrant next-batch scheduling; `31e0e016...` removes direct VBlank vector-copy/allocation. `tests/video_frame_trace_test.cpp` exit 0 tests a host-only fixed ring/overflow/mixed-generation detector; `650db732...` adds `GAMEYOB_VIDEO_TRACE`. `VIDEO_TRACE=1` diagnostic NDS build at source `53692f5a09b3c7006040d7f2ac2ed1df3bb8f216` passed DS build/binary checks (`36180800999`); no hardware launch/CSV trace. ARM7/ARM9 remap race remains a source hypothesis only | USER_REPORTED symptom; cause UNCONFIRMED, no cause-linked fix or integrated flicker reproduction. Host queue/trace tests and diagnostic build are not a reproduced DS flicker nor proof of DMA safety. No claim of flicker fixed |
| Printer parser + BMP synthetic path | Valid packet sequence creates expected BMP pixels; invalid checksum/length/RLE cannot mutate retained image or trigger print; failed writes preserve previous output | At d73, `gbprinter_test.exe` exit 0 tested BMP header/pixels, checksum/length, truncated RLE, injected short-write/flush/rename failures, existing-file retention, retry/reset/cwd pinning and valid `0xFF`→129. Follow-up test expected 12 encoded bytes to expand to 774 and failed on d73 (assertion exit `-1073740791`), exposing the 640-byte decoded packet cap despite `MAX_IMAGE=8000`. At current SHA `53692f5a09b3c7006040d7f2ac2ed1df3bb8f216`, two-pass code validates checksum/RLE/expanded length against remaining 8,000-byte capacity, then decodes directly to image tail (avoids an 8 KiB stack duplicate). Windows Clang parser/BMP and production serial tests exit 0; positive 12-byte→774-byte case, exact 8,000-byte total, over-cap rejection and valid-prefix/truncated-tail transactional preservation are covered | Previously identified P1 corrected; scoped regression and current-SHA CI pass. Still no actual Tales game/menu, production DS FAT/FileHandle/Unicode/CP949/8.3 output or hardware |
| Printer FF01/FF02 / serial completion | Internal printer transfer completes and clears SC start bit; linked Game Boy peer pending transfers keep their start bit until their own transfer completion | At `6e6cfda`, synthetic GB/CGB test passed but code review found SC clear could affect an unrelated linked-peer transfer. Commit `d73d7067285842f132771eed52dffef493bfa0b0` narrows SC clear. Expanded `gbprinter_serial_integration_test.exe` on d73 Windows Clang exit 0 asserts peer SC remains armed before exchange and both SC/SB complete after it, plus printer INIT/STATUS/DATA/end/PRINT → BMP and one completion IRQ/no second-frame IRQ. Current `53692f5a09b3c7006040d7f2ac2ed1df3bb8f216` serial test also passes with the expanded RLE cases | Scoped production-host tests and current-SHA CI PASS as reported. No actual Tales menu, physical DS timing or device test. Host harness uses lifecycle-test platform/FileHandle boundary |
| Printer output path | Storage basename captured at ROM selection; successful staged output preserves old file on failure and survives menu cwd changes | Synthetic Windows temp directory output; expected names `<ROM-storage-basename>-N.bmp`, staged through `.tmp` before replacement. Test injects short write/flush/rename failures and checks retention. Diagnostics require **Console Output → Debug**; `printLog` has no guaranteed visible error UI. No production DS `FileHandle`/FAT, Unicode/CP949/8.3 alias or actual SD-card write | Host filesystem behavior only; real DS FileHandle/FAT/SD and user's game NOT RUN. Never use user printouts for write tests |
| Legacy lifecycle / sanitizer / package / DS builds | BASE regressions stay passing on the final candidate, with required checks repeated at exact source SHA | At current `53692f5a09b3c7006040d7f2ac2ed1df3bb8f216`: core `36180780686`, `36180775281`, DS `36180780454`, `36180775148`, preflight `36180797959` PASS; separate diagnostic DS build `36180800999` PASS. Preflight includes normal/UBSan/ASan+LSan, 27 host tests, package/resources, two clean pinned BlocksDS 1.22.2 builds and binary/header/embedded-asset checks. Downloaded artifact hashes/sizes follow. Full Galmuri regeneration NOT RUN. | Required named software/build gates PASS at exact source SHA; this is not real-device or full-game evidence. Hardware remains pending |

## Build identity and remaining release gates

At corrected source SHA `53692f5a09b3c7006040d7f2ac2ed1df3bb8f216`, normal NDS artifacts are 731,648 bytes each: `gameyob.nds` SHA-256 `9cc7070abe6e783188ed04a2c936547a44a1e05fa0eee8a33fd0963f6d15e03a`; `gameyob_dsi.nds` `20d4fa8bfcb34b385f9cb93961750cef83c6efcc2d1672f5099aac309509f09d`. Diagnostic artifacts are 733,696 bytes each: `gameyob.nds` `7ff50cdc73bb82b4af7c16109da9401a189b091b31b66d67b8730b706ad91201`; `gameyob_dsi.nds` `b6a0544103373a297d582dfa4a2667172283c52265b7bacdfe63904c7a7c955a`. Diagnostic ZIP with notices is 798,298 bytes, SHA-256 `8635778e0af057dba8787dea9986411c4bc101a3c6877c42c52867ba8d6a0dd8`; it was extracted and verified. The earlier diagnostics ZIP without notices (`diagnostics-d73d706.zip`, 791,921 bytes, SHA-256 `e796a79ada99b13b0ee709346d129c5d9a5524521301b45d7468d7e02566c4fa`) must NOT be distributed. The correctly noticed d73 ZIP (`diagnostics-d73d706-with-notices.zip`, 798,012 bytes, SHA-256 `8af9572494422ad9239b01cacc9e4b21e96465bd75615804b0c6e3fc6159c484`) is also historical, predates the RLE fix, and is not an artifact for source 53692f5. All diagnostic artifacts are distinct from normal emulator output and are not hardware validation.

User-facing performance measurement (guest cycles/frames per real second, display FPS, waits, CPU load) was not run, so no speed percentage is supported. No actual DS/3DS game launch, Tales menu print attempt, FAT/Unicode/CP949/8.3 path test, or diagnostic CSV capture was recorded. Flicker remains USER_REPORTED with source hypotheses only and no cause-linked safe correction; it therefore remains a publication blocker. The repository's inherited provenance note at `docs/maintenance/repository-preflight-20260903.md` lines 223–226 records `3in1.*` attribution without a separate grant. No new dependencies/assets were added in this task, and existing package notices were included in checks; this report does not make a legal-clearance determination. This unresolved provenance is also a publication gate pending maintainer resolution. No tag or Release was created by this evidence work.

References: Pan Docs Game Boy Printer page at repository revision `e196504` (2026-04-19), <https://gbdev.io/pandocs/Gameboy_Printer.html>; Nintendo *Game Boy Programming Manual*, Chapter 9 printed page 244, <https://thissideout.wordpress.com/wp-content/uploads/2014/02/gameboyprogrammingmanual.pdf>. The Pan Docs page was search-index accessible but direct fetch returned HTTP 403 during this audit; checked-in revision/source was used. It specifies packet fields, checksum over command/flags/length/payload (excluding magic/checksum), status after the two trailing zero bytes, INIT buffer clear, DATA chunks up to `0x280` bytes, PRINT's four parameters, zero-sheet line feed, required empty DATA packet before PRINT, 8 KiB printer RAM / 160×200 maximum bitmap area, and status-bit meanings. The Nintendo manual Chapter 9 p.244 confirms `0xFF` is a valid RLE control repeating the next byte 129 times; the earlier owned-worktree parser rejection was incorrect and was corrected in integration source and fixture. Pan Docs itself leaves compression details incomplete.

The parser intentionally has bounded packet/image storage, checksum validation before command effects, and staged BMP replacement according to its synthetic tests. Current implementation rejects sheet counts other than one, creates a separate BMP for each accepted PRINT, and does not render margins/exposure as paper effects. The d73 decoder's 640-byte expanded-packet cap was corrected in `53692f5a09b3c7006040d7f2ac2ed1df3bb8f216` with a two-pass validate-then-decode implementation, bounded by remaining 8,000-byte image capacity; current source and CI include above-640, total-bound and truncation-preservation tests. It models only the status subset implemented in code; it does not claim physical battery, jam, thermal, paper, or timing behavior. Do not describe broader hardware compatibility.

## Required evidence to close

Windows Clang tests are owner-reported with the production test command pattern recorded above; individual raw logs and repeat counts are not in the supplied evidence. At d73d706, the expanded serial test checks the linked-peer pending-transfer counterfactual missed earlier; the decoder correction and current-SHA CI now pass at `53692f5a09b3c7006040d7f2ac2ed1df3bb8f216`. Remaining distinctions: the user-reported Tales game/menu flow; host filesystem versus DS FAT/FileHandle; SGB guest progress versus display FPS and limiter waits; intended/filter/camera blacking versus incomplete frame publication; DS host ordering versus physical VRAM/DMA timing. The current `VIDEO_TRACE=1` diagnostic build and **Debug → Video Trace Dump** path passed build/binary checks, but no hardware launch or CSV capture occurred. Buffer holds 256 events and can overwrite older ones. Hardware remains pending. The absence of a game run or trace is NOT RUN, never PASS.

## Printer protocol and implementation boundary

No release/version/tag/publish conclusion belongs in this draft. The release gate remains blocked until each of the three reported problems has an evidenced cause-linked correction and meaningful regression test, integrated checks pass, and an independent final review is complete.
